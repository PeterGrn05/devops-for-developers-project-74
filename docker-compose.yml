services:
  app:
    image: petergrn05/devops-for-developers-project-74
    build: 
      context: .
      dockerfile: Dockerfile.production
    working_dir: /app
    environment:
      - NODE_ENV=development
    command: make test